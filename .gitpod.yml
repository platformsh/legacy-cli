image:
  file: .gitpod/.gitpod.Dockerfile
tasks:
  - name: Prepare Environment
    init: |
      composer install
      composer update-countries
    command: | 
      ./.gitpod/scripts/login.sh
      printf "\n\n All set! Be sure to test your changes by running: vendor/bin/phpunit\n\n"
