image:
  file: .gitpod/.gitpod.Dockerfile
tasks:
  - name: Prepare Environment
    init: |
      composer install
      composer update-countries
    command: | 
      ./.gitpod/scripts/alias-dev-cli.sh
      ./.gitpod/scripts/login.sh
